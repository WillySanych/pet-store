apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "petstore.name" . }}
  labels: {{- include "petstore.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
